---
interface Props {
    value: string;
}

const { value } = Astro.props;
---

<div class="surface-card py-[18px] px-5">
    <div class="text-[27px] font-bold text-accent glow">{value}</div>
    <div class="text-[11.5px] text-body mt-[5px] leading-normal"><slot /></div>
</div>
